#ifndef FLAGS_H
#define FLAGS_H

// TODO: maybe not the best design choice
//  but these flags are actually outside of the CHIP-8 domain

extern bool shutdown_flag;
extern bool reset_flag;

#endif // FLAGS_H